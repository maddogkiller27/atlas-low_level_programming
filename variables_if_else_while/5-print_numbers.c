#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - prints numbers 1-9
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int numbr;

for (numbr = 0; numbr < 10; numbr++)
printf("%d", numbr);

printf("\n");

return (0);
}
