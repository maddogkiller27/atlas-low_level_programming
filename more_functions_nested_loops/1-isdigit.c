#include "main.h"
#include <stdio.h>

/**
 * _isdigit - checks if digit or not
 * @c: input being checked
 *
 * Return: 1 if digit, 0 if not
 */

int _isdigit(int c);
{
if (c >= '0' && c <= '9')
return (1);
else
return (0);
}
